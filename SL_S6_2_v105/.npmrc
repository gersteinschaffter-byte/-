; Keep default public registry for local development.
; In restricted networks, set NPM_REGISTRY_URL and run install with:
;   npm config set registry "$NPM_REGISTRY_URL" --location project
registry=https://registry.npmjs.org/
