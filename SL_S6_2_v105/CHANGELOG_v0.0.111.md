# CHANGELOG v0.0.111

## 调整目标（单人本地测试优先）

- 暂时下调 `check` 门槛为仅执行 `typecheck`，确保在依赖不可安装的网络环境下仍可继续开发与自测。
- 保留 `check:full`（`typecheck + lint + build`）作为后续网络可用时的一键全量门禁。
- 移除上一轮仅用于受限网络自举的脚本与文档，避免单人开发流程复杂化。
