# v0.0.64

## 修复
- **ScrollView 触摸滑动很难 / 容易误触**：加入拖拽阈值（超过一定距离才判定为滚动），滚动时临时禁用内容子节点交互并吞掉 pointerup，避免“滑着滑着点到按钮/卡片”。
- 增强 Pixi v7 的触摸输入稳定性：为 ScrollView 设置 `eventMode='static'` + `hitArea`，让移动端/Modal 内滚动更跟手。

## 影响范围
- 英雄详情属性区滚动
- 召唤池「概率 & 奖池预览」滚动

